{
  "name": "@yeonjae/canon",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Canon pipeline pieces that run before the atomic commit: delta verification (schema, evidence, change-class and frame rules) and the acceptance orchestration that calls canon.commit_delta.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b"
  },
  "dependencies": {
    "@yeonjae/db": "workspace:*",
    "@yeonjae/domain": "workspace:*",
    "@yeonjae/prose": "workspace:*"
  }
}
