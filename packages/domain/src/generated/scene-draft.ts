/* eslint-disable */
// GENERATED by tools/gen-types.ts from schemas/*.schema.json — do not edit. Run `pnpm gen:types`.

/**
 * Prose plus annotations that travel with it so deterministic checks can run without re-parsing dialogue attribution. text is manuscript prose in the project manuscript language (OUTPUT-LANG-001, ADR-0054); offsets are Unicode code points (ADR-0030).
 */
export interface SceneDraftWriterOutputEnvelope {
  scene_no: number;
  /**
   * The manuscript language. Composition is direct in this language (ADR-0054); English is an explicit later translation surface. Enforced per project by the identity contract.
   */
  language: 'en' | 'ko';
  /**
   * English scene prose
   */
  text: string;
  length?: LengthModel;
  /**
   * Paragraph boundaries as code-point offsets into text; ids are stable within the version.
   */
  paragraphs: {
    id: string;
    start: number;
    end: number;
    kind: 'narration' | 'dialogue' | 'monologue' | 'system_block' | 'mixed';
    /**
     * Deliberate repetition for effect (≤2 per chapter)
     */
    stylistic_repeat?: boolean;
  }[];
  speaker_annotations: {
    utterance_start: number;
    utterance_end: number;
    /**
     * UUIDv7
     */
    speaker_id: string;
    /**
     * Items: UUIDv7
     */
    addressee_ids?: string[];
    intentional_shift?: {
      reason:
        | 'anger'
        | 'intimacy_step'
        | 'disguise'
        | 'mockery'
        | 'public_formality'
        | 'age_reveal'
        | 'emotional_outburst';
      note?: string;
    };
    is_monologue?: boolean;
    intended_register?: DialogueRegister;
  }[];
  /**
   * Facts/events the scene asserts, as the writer understands them; used by pre-pass and for 'fact-bearing span' detection.
   */
  claims: {
    statement: string;
    paragraph_id: string;
    /**
     * Items: UUIDv7
     */
    entity_ids?: string[];
    /**
     * See ADR-0007 and ADR-0039. Fact-bearing frames: canonical and flashback (main or current timeline), prior_loop, alternate_timeline and source_story (each on its own timeline). lie/dream/hallucination/hypothetical/prediction produce knowledge only; plan lives only in plan tables; non_canonical_draft never enters canon.
     */
    frame?:
      | 'canonical'
      | 'flashback'
      | 'dream'
      | 'hallucination'
      | 'lie'
      | 'hypothetical'
      | 'prediction'
      | 'plan'
      | 'prior_loop'
      | 'alternate_timeline'
      | 'source_story'
      | 'non_canonical_draft';
  }[];
  system_blocks?: {
    paragraph_id: string;
    kind:
      'status_window' | 'system_message' | 'ranking_board' | 'community_post' | 'letter' | 'other';
  }[];
  /**
   * Optional; never enters canon
   */
  writer_notes?: string;
}
/**
 * Language-neutral length measurement (ADR-0034). For English manuscripts, words is the author-facing primary unit.
 */
export interface LengthModel {
  words: number;
  /**
   * Unicode code points after NFC, including spaces, excluding markup
   */
  code_points: number;
  paragraphs?: number;
  sentences?: number;
  est_tokens?: number;
  est_tokens_model?: string;
  est_reading_seconds?: number;
}
/**
 * Abstract canonical description of how a speaker addresses a counterpart, rendered in natural English by the writer (replaces Korean speech-level enforcement).
 */
export interface DialogueRegister {
  /**
   * Abstract register axis value (0 = lowest/most casual, 4 = highest/most formal or deferential)
   */
  formality?: number;
  /**
   * Abstract register axis value (0 = lowest/most casual, 4 = highest/most formal or deferential)
   */
  deference?: number;
  /**
   * Abstract register axis value (0 = lowest/most casual, 4 = highest/most formal or deferential)
   */
  familiarity?: number;
  /**
   * Abstract register axis value (0 = lowest/most casual, 4 = highest/most formal or deferential)
   */
  intimacy?: number;
  /**
   * Abstract register axis value (0 = lowest/most casual, 4 = highest/most formal or deferential)
   */
  directness?: number;
  contractions?: 'avoid' | 'neutral' | 'free';
  hedging?: 'high' | 'medium' | 'low';
  /**
   * English vocatives/references the speaker uses for the counterpart, e.g. 'Senior Park', 'sir', 'Do-yoon'
   */
  address_terms?: string[];
  titles?: string[];
  /**
   * Overrides applied in public settings
   */
  public_variant?: {
    /**
     * Abstract register axis value (0 = lowest/most casual, 4 = highest/most formal or deferential)
     */
    formality?: number;
    address_terms?: string[];
    titles?: string[];
  };
  note?: string;
}
