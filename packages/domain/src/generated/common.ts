/* eslint-disable */
// GENERATED by tools/gen-types.ts from schemas/*.schema.json — do not edit. Run `pnpm gen:types`.

/**
 * UUIDv7
 *
 * This interface was referenced by `CommonDefinitions`'s JSON-Schema
 * via the `definition` "uuid".
 */
export type Uuid = string;
/**
 * UTC ISO-8601
 *
 * This interface was referenced by `CommonDefinitions`'s JSON-Schema
 * via the `definition` "timestamp".
 */
export type Timestamp = string;
/**
 * BCP 47 language tag, e.g. en, en-US, en-GB, ko
 *
 * This interface was referenced by `CommonDefinitions`'s JSON-Schema
 * via the `definition` "languageCode".
 */
export type LanguageCode = string;
/**
 * The manuscript language. Composition is direct in this language (ADR-0054); English is an explicit later translation surface. Enforced per project by the identity contract.
 *
 * This interface was referenced by `CommonDefinitions`'s JSON-Schema
 * via the `definition` "manuscriptLanguage".
 */
export type ManuscriptLanguage = 'en' | 'ko';
/**
 * Spelling and punctuation locale for the manuscript language (en-US, en-GB, ko-KR)
 *
 * This interface was referenced by `CommonDefinitions`'s JSON-Schema
 * via the `definition` "spellingLocale".
 */
export type SpellingLocale = 'en-US' | 'en-GB' | 'ko-KR';
/**
 * UTF-8, NFC-normalized text. Working text (plans, canon statements, issues) is English; the language of user-authored text is recorded alongside it where it may vary.
 *
 * This interface was referenced by `CommonDefinitions`'s JSON-Schema
 * via the `definition` "text".
 */
export type Text = string;
/**
 * See ADR-0007 and ADR-0039. Fact-bearing frames: canonical and flashback (main or current timeline), prior_loop, alternate_timeline and source_story (each on its own timeline). lie/dream/hallucination/hypothetical/prediction produce knowledge only; plan lives only in plan tables; non_canonical_draft never enters canon.
 *
 * This interface was referenced by `CommonDefinitions`'s JSON-Schema
 * via the `definition` "realityFrame".
 */
export type RealityFrame =
  | 'canonical'
  | 'flashback'
  | 'dream'
  | 'hallucination'
  | 'lie'
  | 'hypothetical'
  | 'prediction'
  | 'plan'
  | 'prior_loop'
  | 'alternate_timeline'
  | 'source_story'
  | 'non_canonical_draft';
/**
 * This interface was referenced by `CommonDefinitions`'s JSON-Schema
 * via the `definition` "severity".
 */
export type Severity = 'blocking' | 'major' | 'minor' | 'note';
/**
 * This interface was referenced by `CommonDefinitions`'s JSON-Schema
 * via the `definition` "confidence".
 */
export type Confidence = number;
/**
 * Separate evaluation dimensions (EVAL-SEPARATION-001). prose = English prose quality; structure = Korean-webnovel structural adherence.
 *
 * This interface was referenced by `CommonDefinitions`'s JSON-Schema
 * via the `definition` "qualityDimension".
 */
export type QualityDimension =
  | 'prose'
  | 'structure'
  | 'genre'
  | 'voice'
  | 'continuity'
  | 'knowledge'
  | 'promise'
  | 'contract'
  | 'repetition'
  | 'length'
  | 'output_language';
/**
 * This interface was referenced by `CommonDefinitions`'s JSON-Schema
 * via the `definition` "stance".
 */
export type Stance =
  'knows' | 'suspects' | 'believes_false' | 'pretends' | 'unaware' | 'forgot' | 'doubts';
/**
 * Abstract register axis value (0 = lowest/most casual, 4 = highest/most formal or deferential)
 *
 * This interface was referenced by `CommonDefinitions`'s JSON-Schema
 * via the `definition` "registerLevel".
 */
export type RegisterLevel = number;
/**
 * This interface was referenced by `CommonDefinitions`'s JSON-Schema
 * via the `definition` "registerShiftReason".
 */
export type RegisterShiftReason =
  | 'anger'
  | 'intimacy_step'
  | 'disguise'
  | 'mockery'
  | 'public_formality'
  | 'age_reveal'
  | 'emotional_outburst';
/**
 * This interface was referenced by `CommonDefinitions`'s JSON-Schema
 * via the `definition` "requirementKind".
 */
export type RequirementKind = 'hard' | 'soft' | 'assumption';
/**
 * This interface was referenced by `CommonDefinitions`'s JSON-Schema
 * via the `definition` "provenance".
 */
export type Provenance =
  | 'user'
  | 'system_default'
  | 'model_inferred'
  | 'bible'
  | 'extraction'
  | 'user_correction'
  | 'retcon'
  | 'rollback';
/**
 * Dependency edge class (ADR-0032). Only material edges mark dependents stale by default.
 *
 * This interface was referenced by `CommonDefinitions`'s JSON-Schema
 * via the `definition` "materiality".
 */
export type Materiality = 'material' | 'contextual';
/**
 * Timeline kinds (ADR-0023, ADR-0039). prior_loop, alternate and source_story timelines carry their own facts; the main timeline learns of them only through knowledge stances.
 *
 * This interface was referenced by `CommonDefinitions`'s JSON-Schema
 * via the `definition` "timelineKind".
 */
export type TimelineKind = 'main' | 'prior_loop' | 'alternate' | 'source_story';
/**
 * Manuscript-version lifecycle (ADR-0037): working (draft/candidate/revision under production) → approved (approval-locked for extraction; immutable input to canon extraction) → accepted (set inside the atomic canon commit) → superseded (a regeneration was accepted) | retconned (a retcon version was accepted); rejected versions move to quarantine.
 *
 * This interface was referenced by `CommonDefinitions`'s JSON-Schema
 * via the `definition` "manuscriptStatus".
 */
export type ManuscriptStatus =
  'working' | 'approved' | 'accepted' | 'superseded' | 'retconned' | 'rejected';
/**
 * How a manuscript version came to exist (content provenance, orthogonal to status): assembled from scene drafts, produced by a patch (revision), one of N candidates, a retcon edit of an accepted version, or imported text.
 *
 * This interface was referenced by `CommonDefinitions`'s JSON-Schema
 * via the `definition` "manuscriptOrigin".
 */
export type ManuscriptOrigin = 'assembled' | 'revision' | 'candidate' | 'retcon' | 'imported';

/**
 * Shared primitive types referenced by all Yeonjae Studio schemas. Text fields are language-neutral; where the language may vary, an explicit language code accompanies the text. Manuscript text follows the project manuscript language, en or ko (OUTPUT-LANG-001, ADR-0054).
 */
export interface CommonDefinitions {
  [k: string]: unknown | undefined;
}
/**
 * User-authored text with its language code and an optional English working paraphrase used in prompts.
 *
 * This interface was referenced by `CommonDefinitions`'s JSON-Schema
 * via the `definition` "localizedText".
 */
export interface LocalizedText {
  text: Text;
  language: LanguageCode;
  /**
   * English working paraphrase when language != en (never manuscript text)
   */
  text_en?: string;
}
/**
 * Language-neutral length measurement (ADR-0034). For English manuscripts, words is the author-facing primary unit.
 *
 * This interface was referenced by `CommonDefinitions`'s JSON-Schema
 * via the `definition` "lengthModel".
 */
export interface LengthModel {
  words: number;
  /**
   * Unicode code points after NFC, including spaces, excluding markup
   */
  code_points: number;
  paragraphs?: number;
  sentences?: number;
  est_tokens?: number;
  est_tokens_model?: string;
  est_reading_seconds?: number;
}
/**
 * Author-facing length target. Unit is words for English, characters for Korean (ADR-0054; characters are Unicode code points excluding newlines, spaces included); tolerance is a ratio.
 *
 * This interface was referenced by `CommonDefinitions`'s JSON-Schema
 * via the `definition` "lengthTarget".
 */
export interface LengthTarget {
  unit: 'words' | 'characters';
  value: number;
  tolerance_ratio?: number;
}
/**
 * Position in story time (ADR-0040). Two orderings: NARRATIVE order (chapter_no, ordinal) is total within a timeline and always present; WORLD order (calendar + world_date) is partial and used only when both clocks share a calendar and neither has precision=unknown. Facts and events sort by narrative order (derived key ord = chapter_no*1e6 + ordinal); world_date resolves flashbacks and elapsed-time checks. Clocks on different timelines are comparable only through the child timeline's divergence clock.
 *
 * This interface was referenced by `CommonDefinitions`'s JSON-Schema
 * via the `definition` "storyClock".
 */
export interface StoryClock {
  /**
   * Chapter during whose story-present this moment occurs. 0 = before chapter 1 (backstory, flashback material, prior-loop or source-story events whose story-present precedes the manuscript).
   */
  chapter_no: number;
  /**
   * Order within the chapter's story-present, assigned by the extractor (paragraph order of the anchoring evidence by default). Equal ordinals mean simultaneous or unordered; ties break deterministically by item id.
   */
  ordinal: number;
  /**
   * Calendar system of world_date: 'gregorian', 'relative_days' (D+35 / D-10 from the story's day zero), or 'era:<name>' for in-world calendars. world_date values are comparable only within one calendar.
   */
  calendar?: string;
  /**
   * In-world date/time in the stated calendar (ISO-8601 for gregorian; D±n[Thh:mm] for relative_days; free era-relative string otherwise)
   */
  world_date?: string;
  /**
   * Certainty of world_date. unknown = no usable world position (narrative order only; reported as a continuity risk when a duration check needs it).
   */
  precision: 'exact' | 'approx' | 'unknown';
  /**
   * Half-width of the plausible window when precision=approx
   */
  uncertainty_days?: number;
}
/**
 * Exact span into an immutable manuscript version. start/end are Unicode code-point offsets into the NFC text (ADR-0030); quote must equal the code-point slice text[start:end].
 *
 * This interface was referenced by `CommonDefinitions`'s JSON-Schema
 * via the `definition` "evidenceRef".
 */
export interface EvidenceRef {
  manuscript_version_id: Uuid;
  chapter_no?: number;
  /**
   * Stable paragraph id within the version, e.g. p14
   */
  paragraph_id?: string;
  /**
   * Code-point offset (inclusive)
   */
  start: number;
  /**
   * Code-point offset (exclusive)
   */
  end: number;
  quote: Text;
  /**
   * SHA-256 of quote
   */
  quote_hash?: string;
}
/**
 * This interface was referenced by `CommonDefinitions`'s JSON-Schema
 * via the `definition` "entityRef".
 */
export interface EntityRef {
  entity_id: Uuid;
  display_name?: Text;
}
/**
 * A character entity or a pseudo-knower.
 *
 * This interface was referenced by `CommonDefinitions`'s JSON-Schema
 * via the `definition` "knowerRef".
 */
export interface KnowerRef {
  kind: 'character' | 'narrator' | 'reader';
  entity_id?: Uuid;
}
/**
 * Abstract canonical description of how a speaker addresses a counterpart, rendered in natural English by the writer (replaces Korean speech-level enforcement).
 *
 * This interface was referenced by `CommonDefinitions`'s JSON-Schema
 * via the `definition` "dialogueRegister".
 */
export interface DialogueRegister {
  formality?: RegisterLevel;
  deference?: RegisterLevel;
  familiarity?: RegisterLevel;
  intimacy?: RegisterLevel;
  directness?: RegisterLevel;
  contractions?: 'avoid' | 'neutral' | 'free';
  hedging?: 'high' | 'medium' | 'low';
  /**
   * English vocatives/references the speaker uses for the counterpart, e.g. 'Senior Park', 'sir', 'Do-yoon'
   */
  address_terms?: string[];
  titles?: string[];
  /**
   * Overrides applied in public settings
   */
  public_variant?: {
    formality?: RegisterLevel;
    address_terms?: string[];
    titles?: string[];
  };
  note?: string;
}
/**
 * Pinned input versions for reproducibility.
 *
 * This interface was referenced by `CommonDefinitions`'s JSON-Schema
 * via the `definition` "versionRef".
 */
export interface VersionRef {
  spec_version?: number;
  bible_version?: number;
  narrative_identity_version_id?: Uuid;
  canon_version?: number;
  prompt_set_id?: Uuid;
  template_version?: string;
}
