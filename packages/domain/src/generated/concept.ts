/* eslint-disable */
// GENERATED by tools/gen-types.ts from schemas/*.schema.json — do not edit. Run `pnpm gen:types`.

/**
 * One story concept candidate produced by concept_generator (FR-2.1). Candidates are compared pairwise; the selected/merged concept seeds the Story Bible and Series Blueprint.
 */
export interface ConceptCandidate {
  /**
   * UUIDv7
   */
  id: string;
  /**
   * UUIDv7
   */
  project_id: string;
  spec_version: number;
  /**
   * Diversity seed used for generation, e.g. satisfaction-driven / emotion-driven / mystery-driven
   */
  angle: string;
  /**
   * UTF-8, NFC-normalized text. Working text (plans, canon statements, issues) is English; the language of user-authored text is recorded alongside it where it may vary.
   */
  logline: string;
  /**
   * UTF-8, NFC-normalized text. Working text (plans, canon statements, issues) is English; the language of user-authored text is recorded alongside it where it may vary.
   */
  story_promise: string;
  /**
   * UTF-8, NFC-normalized text. Working text (plans, canon statements, issues) is English; the language of user-authored text is recorded alongside it where it may vary.
   */
  reader_fantasy: string;
  /**
   * UTF-8, NFC-normalized text. Working text (plans, canon statements, issues) is English; the language of user-authored text is recorded alongside it where it may vary.
   */
  main_conflict: string;
  protagonist_sketch?: string;
  /**
   * UTF-8, NFC-normalized text. Working text (plans, canon statements, issues) is English; the language of user-authored text is recorded alongside it where it may vary.
   */
  chapter_one_hook: string;
  /**
   * UTF-8, NFC-normalized text. Working text (plans, canon statements, issues) is English; the language of user-authored text is recorded alongside it where it may vary.
   */
  ending_direction: string;
  /**
   * How power/status rises across the series, with rough milestones
   */
  progression_curve?: string;
  /**
   * @minItems 1
   */
  differentiators: [string, ...string[]];
  genre_fit_notes: string[];
  risk_notes: string[];
  /**
   * How each hard requirement is honored by this concept
   */
  requirement_coverage?: {
    requirement_id: string;
    how: string;
  }[];
  status: 'candidate' | 'selected' | 'merged_into' | 'rejected';
  /**
   * UUIDv7
   */
  merged_into_id?: string;
  /**
   * UUIDv7
   */
  generator_call_id?: string;
}
