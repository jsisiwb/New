/* eslint-disable */
// GENERATED by tools/gen-types.ts from schemas/*.schema.json — do not edit. Run `pnpm gen:types`.

/**
 * Major or minor arc plan (docs/03-story-planning/01 §4). Plan frame — never canon.
 */
export interface ArcPlan {
  /**
   * UUIDv7
   */
  id: string;
  /**
   * UUIDv7
   */
  project_id: string;
  /**
   * UUIDv7
   */
  season_id: string;
  parent_arc_id?: string | null;
  kind: 'major' | 'minor';
  ordinal: number;
  version?: number;
  pinned?: VersionRef;
  title: string;
  objective: string;
  conflict: string;
  antagonistic_force?: string;
  stakes?: string;
  entry_state?: string;
  exit_state_assertions?: string[];
  /**
   * Items: UUIDv7
   */
  participants?: string[];
  /**
   * Items: UUIDv7
   */
  locations?: string[];
  story_time_window?: {
    start?: StoryClock;
    end?: StoryClock;
  };
  chapter_range_est: {
    from: number;
    to: number;
  };
  /**
   * @minItems 1
   */
  beats: [
    {
      id: string;
      type:
        | 'setup'
        | 'escalation'
        | 'reversal'
        | 'cider'
        | 'revelation'
        | 'emotional'
        | 'progression'
        | 'climax'
        | 'aftermath'
        | 'comedic'
        | 'relationship';
      description: string;
      target_chapter_offset: number;
      /**
       * Items: UUIDv7
       */
      participants?: string[];
      /**
       * Items: UUIDv7
       */
      promise_refs?: string[];
      knowledge_changes_planned?: {
        knower: KnowerRef;
        /**
         * proposition id or 'new:<text>'
         */
        proposition_ref: string;
        to_stance:
          'knows' | 'suspects' | 'believes_false' | 'pretends' | 'unaware' | 'forgot' | 'doubts';
        channel?: string;
      }[];
      status?: 'planned' | 'realized' | 'partially_realized' | 'unrealized';
    },
    ...{
      id: string;
      type:
        | 'setup'
        | 'escalation'
        | 'reversal'
        | 'cider'
        | 'revelation'
        | 'emotional'
        | 'progression'
        | 'climax'
        | 'aftermath'
        | 'comedic'
        | 'relationship';
      description: string;
      target_chapter_offset: number;
      /**
       * Items: UUIDv7
       */
      participants?: string[];
      /**
       * Items: UUIDv7
       */
      promise_refs?: string[];
      knowledge_changes_planned?: {
        knower: KnowerRef;
        /**
         * proposition id or 'new:<text>'
         */
        proposition_ref: string;
        to_stance:
          'knows' | 'suspects' | 'believes_false' | 'pretends' | 'unaware' | 'forgot' | 'doubts';
        channel?: string;
      }[];
      status?: 'planned' | 'realized' | 'partially_realized' | 'unrealized';
    }[],
  ];
  /**
   * Items: UUIDv7
   */
  promises_opened?: string[];
  /**
   * Items: UUIDv7
   */
  promises_advanced?: string[];
  /**
   * Items: UUIDv7
   */
  promises_paid?: string[];
  progression_milestone_ids?: string[];
  relationship_milestone_ids?: string[];
  cadence_check?: {
    cider_interval_ok?: boolean;
    progression_interval_ok?: boolean;
    frustration_streak_ok?: boolean;
    notes?: string[];
  };
  risks?: string[];
  must_not?: string[];
  repetition_check?: {
    /**
     * Items: UUIDv7
     */
    compared_arc_ids?: string[];
    similarity_score?: number;
    notes?: string;
  };
  /**
   * Plan lifecycle (ADR-0037): locked = approved and frozen for production.
   */
  status: 'draft' | 'validated' | 'locked' | 'stale' | 'superseded' | 'realized';
  stale_reasons?: string[];
}
/**
 * Pinned input versions for reproducibility.
 */
export interface VersionRef {
  spec_version?: number;
  bible_version?: number;
  /**
   * UUIDv7
   */
  narrative_identity_version_id?: string;
  canon_version?: number;
  /**
   * UUIDv7
   */
  prompt_set_id?: string;
  template_version?: string;
}
/**
 * Position in story time (ADR-0040). Two orderings: NARRATIVE order (chapter_no, ordinal) is total within a timeline and always present; WORLD order (calendar + world_date) is partial and used only when both clocks share a calendar and neither has precision=unknown. Facts and events sort by narrative order (derived key ord = chapter_no*1e6 + ordinal); world_date resolves flashbacks and elapsed-time checks. Clocks on different timelines are comparable only through the child timeline's divergence clock.
 */
export interface StoryClock {
  /**
   * Chapter during whose story-present this moment occurs. 0 = before chapter 1 (backstory, flashback material, prior-loop or source-story events whose story-present precedes the manuscript).
   */
  chapter_no: number;
  /**
   * Order within the chapter's story-present, assigned by the extractor (paragraph order of the anchoring evidence by default). Equal ordinals mean simultaneous or unordered; ties break deterministically by item id.
   */
  ordinal: number;
  /**
   * Calendar system of world_date: 'gregorian', 'relative_days' (D+35 / D-10 from the story's day zero), or 'era:<name>' for in-world calendars. world_date values are comparable only within one calendar.
   */
  calendar?: string;
  /**
   * In-world date/time in the stated calendar (ISO-8601 for gregorian; D±n[Thh:mm] for relative_days; free era-relative string otherwise)
   */
  world_date?: string;
  /**
   * Certainty of world_date. unknown = no usable world position (narrative order only; reported as a continuity risk when a duration check needs it).
   */
  precision: 'exact' | 'approx' | 'unknown';
  /**
   * Half-width of the plausible window when precision=approx
   */
  uncertainty_days?: number;
}
/**
 * A character entity or a pseudo-knower.
 */
export interface KnowerRef {
  kind: 'character' | 'narrator' | 'reader';
  /**
   * UUIDv7
   */
  entity_id?: string;
}
